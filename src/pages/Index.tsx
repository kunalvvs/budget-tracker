
import { useEffect } from "react";

const Index = () => {
  useEffect(() => {
    // This component doesn't need to do anything special
    // since the HTML/CSS/JS files are handling the budget tracker
  }, []);

  return null;
};

export default Index;
